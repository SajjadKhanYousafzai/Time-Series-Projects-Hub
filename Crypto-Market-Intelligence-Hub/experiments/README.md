# Experiments

This folder stores lightweight experiment outputs generated by `scripts/run_all.py`.

- `summary_by_asset.csv` — per-asset stats (rows, start, end, mean_close)

If you add model training workflows, include per-run directories with `metrics.json`, `params.yaml`, and saved model artifacts.
